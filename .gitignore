node_modules/
.env
*.log
.DS_Store

# runtime state — never commit (lives on a disk / data store in prod)
data/*
!data/.gitkeep
.env*

# calibration data: never commit real faces or renders
scripts/calibrate/out*/
scripts/calibrate/realfaces/**/*.jpg
scripts/calibrate/realfaces/**/*.JPG
scripts/calibrate/realfaces/**/*.jpeg
scripts/calibrate/realfaces/**/*.JPEG
scripts/calibrate/realfaces/**/*.png
scripts/calibrate/realfaces/**/*.heic
scripts/bakeoff/results/

# raw bird refs are regenerable (scripts/gen-bird-cutouts.js) — only ship the cutouts
public/birds/*.jpg
